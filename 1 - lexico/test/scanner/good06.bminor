// calcular pi

calcular_pi: function float () = {
    i: integer;
    pi: float = 0;
    for( i=0; i<1000000; i++ ) {
        pi = pi + 1.0/i;
    }
    return pi;
}

main: function void () = {
    print(calcular_pi());
}